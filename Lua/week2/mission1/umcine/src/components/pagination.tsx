interface PaginationProps { currentPage: number; onPageChange: (page: number) => void; }
export default function Pagination({ currentPage, onPageChange }: PaginationProps) { const pages = [1,2,3,4,5]; return <nav className="pagination" aria-label="페이지 이동"><button type="button" className="page-arrow" aria-label="이전 페이지" disabled={currentPage === 1} onClick={() => onPageChange(Math.max(1,currentPage - 1))}><img src="/icons/chevron-left.svg" alt="" /></button>{pages.map((page) => <button key={page} type="button" className={`page-button ${currentPage === page ? "active" : ""}`} aria-current={currentPage === page ? "page" : undefined} onClick={() => onPageChange(page)}>{page}</button>)}<button type="button" className="page-arrow" aria-label="다음 페이지" disabled={currentPage === 5} onClick={() => onPageChange(Math.min(5,currentPage + 1))}><img src="/icons/chevron-right.svg" alt="" /></button></nav>; }
